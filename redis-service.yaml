apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
spec:
  type: ClusterIP
  selector:
    app: redis          # matches the pod template labels in redis-deployment.yaml
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
    protocol: TCP
